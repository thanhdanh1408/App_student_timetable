name: student_timetable_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^4.0.0
  provider: ^6.1.2
  go_router: ^14.0.0
  shared_preferences: ^2.3.2
  intl: ^0.20.2
  path_provider: ^2.1.4
  flutter_local_notifications: ^17.2.2
  workmanager: ^0.7.0
  permission_handler: ^11.3.1
  timezone: ^0.9.4
  
  # Firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.11

flutter:
  uses-material-design: true
  assets:
    - assets/images/